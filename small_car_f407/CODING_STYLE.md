# 编码规范

本项目采用 Google C/C++ 风格作为人工编写代码的主要规范，并兼容 STM32 HAL/CubeMX 生成代码的既有结构。

## C 语言

- 项目代码使用 C11。
- C、头文件和汇编文件使用 2 个空格缩进，不使用 Tab。
- 单行长度尽量控制在 120 列以内。
- 人工编写的新代码按 Google 风格使用行尾左大括号，例如 `if (...) {`。
- 文件内私有函数和局部变量使用 `snake_case`。
- HAL/CubeMX 回调、初始化函数和已生成接口保持原有 STM32 命名。
- 宏和编译期常量使用 `UPPER_SNAKE_CASE`。
- 文件内私有函数和数据优先使用 `static`。
- 协议、寄存器、打包数据优先使用 `<stdint.h>` 中的固定宽度整数类型。

## STM32CubeMX 生成文件

- 保留 `/* USER CODE BEGIN ... */` 和 `/* USER CODE END ... */` 等生成标记。
- 修改 CubeMX 可能重新生成的文件时，自定义代码只放在用户代码区。
- 不对整份 CubeMX 生成文件做全量格式化，避免产生难以 review 的无关差异。
- CubeMX 自动生成代码可以保留原格式；新增或重写的业务代码再按 Google 风格整理。

## FreeRTOS

- 任务、队列、信号量和定时器命名应清晰表达用途。
- 持有互斥锁或关闭中断时，避免永久阻塞。
- 应用层优先使用 CMSIS-RTOS2 API，只有确有需要时再直接使用 FreeRTOS 原生 API。

## 错误处理

- 外设初始化和运行期 HAL 调用应检查返回值。
- 不可恢复错误统一进入 `Error_Handler()`。
- 中断处理函数保持短小，耗时工作放到任务或回调中处理。

## 格式化

使用仓库中的 `.clang-format` 作为 C/C++ 格式化配置，基础风格为 Google。编辑器通用规则由 `.editorconfig` 提供。只格式化本次实际修改的文件。
